const { Goal } = require('../../models');

const index = () => {
  // get goals limit is 10
};
const show = () => {
  // show goals with query
};
const create = () => {
  // create goals
};
const update = () => {
  // update goals
};
const destroy = () => {
  // delete goals
};

module.exports = {
  index,
  show,
  create,
  update,
  destroy,
};
